apiVersion: v1
kind: Service
metadata:
  name: careflow-api
  namespace: careflow
  labels:
    app.kubernetes.io/name: careflow-api
spec:
  selector:
    app.kubernetes.io/name: careflow-api
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
